# ─────────────────────────────────────────────────────────────────────────────
# Child repos — they are independent git repositories (own .git, own remote,
# own deploy target). The umbrella only tracks orientation docs + the roadmap.
# ─────────────────────────────────────────────────────────────────────────────
Kunfupay-Payins-Back/
Kunfupay-Payins-Front/

# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
.next/
out/
.turbo/

# Tests
coverage/

# Env (each child may also have its own .env.example tracked)
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
*.log

# Vercel
.vercel
